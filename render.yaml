services:
  - type: web
    name: plant-disease-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_PATH
        value: trainedd_model_fixed.keras


startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
